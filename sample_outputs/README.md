# Sample outputs

One subfolder per study, holding a checked-in `report.txt` and `results.csv` from an actual run, so the shape of AxisForge's output is visible without running anything.

Empty until the first study is implemented — see the [studies index](../studies/) for current status.
